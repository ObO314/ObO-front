export interface FormInputs {
    name: string;
    nickname: string;
    email: string;
    password: string;
    confirmPassword: string;
    authMethod: string;
}

export interface InputProps {
    hasError?: boolean;
}
